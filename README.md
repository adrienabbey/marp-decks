# Marp Decks

Reusable Marp themes and Markdown-based presentation decks.

## Structure

- `themes/` — reusable Marp CSS themes
- `presentations/` — individual slide decks
- `.vscode/settings.json` — VS Code Marp theme registration
